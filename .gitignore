# ---------------------------------------------------------------- 密钥与本地配置
# 绝不能进仓库。Key 只从环境变量 / .env / config.json 读（见 README §9）。
# config.example.json 和 .env.example 是模板（占位符），要提交。
.env
config.json

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---------------------------------------------------------------- 运行时产物
# 文件工具的沙箱目录：模型在里面的读写是运行结果，不是源码
workspace/
# 会话与长期记忆的库（默认 ./data/memory.db，见 agent/store.py::default_db_path）。
# 里面是这台机器上的真实对话，跟 config.json 一样属于本地数据。
data/
*.log

# ---------------------------------------------------------------- 测试
# 按仓库主人的要求不提交（README §8 有对应说明）。
# 想让别人能跑测试就删掉这一行。
tests/

# ---------------------------------------------------------------- 图片
# 截图、示意图这类二进制不进版本库。
# 注意没有忽略 .svg：web/static/favicon.svg 是界面真正要用的资源。
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.bmp
screenshots/

# ---------------------------------------------------------------- 本地工具
# Playwright 调试时留下的页面快照和开关记录，属于本机产物。
.playwright-mcp/

# Claude Code 的本地权限记录：里面有本机路径、内网地址、隧道域名这类东西，
# 是「这台机器」的痕迹，不是项目的一部分。
# 它目前靠全局配置（~/.config/git/ignore）挡着，但那层保护不跟着仓库走——
# 换台机器、来个协作者、或者跑在 CI 上就没了。所以在项目里再写一道。
.claude/settings.local.json

# ---------------------------------------------------------------- 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
