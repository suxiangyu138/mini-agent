# 基础 Agent 项目完整设计方案

## 一、项目定位与目标

**项目名**：Mini-Agent（可自定义）

**定位**：一个可运行、可扩展、结构清晰的通用 Agent 框架，作为后续所有高级能力（RAG、多 Agent、规划）的地基。

**核心目标**：
- 具备工具调用能力（Function Calling）
- 支持多轮对话与上下文记忆
- 采用 ReAct 推理循环（推理→行动→观察）
- LLM 后端可插拔，不绑定厂商
- 代码分层清晰，每层可独立替换

**非目标**（明确不做，避免过度设计）：
- 不做分布式、不做高并发
- 不做复杂权限系统
- 不做生产级监控告警（留接口即可）

---

## 二、整体架构

分四层，自上而下依赖，下层不知道上层存在：

```
┌─────────────────────────────────────┐
│           入口层 (main)              │  交互、循环、退出
├─────────────────────────────────────┤
│         核心层 (Agent Core)          │  ReAct 主循环、编排
├──────────────┬──────────────────────┤
│  记忆层       │      工具层           │  上下文管理 / 能力扩展
│  (Memory)    │  (Tools + Registry)  │
├──────────────┴──────────────────────┤
│         模型层 (LLM Adapter)         │  统一模型接口、格式归一化
└─────────────────────────────────────┘
```

**依赖方向**：入口 → 核心 → (记忆 / 工具 / 模型)。核心层是唯一同时用到其他三层的模块。

---

## 三、模块设计

### 1. 模型层（LLM Adapter）

**职责**：屏蔽不同模型厂商的差异，向上提供统一接口。

**统一接口定义**：
- 输入：消息列表 + 工具描述列表（可选）
- 输出：统一结构 `{ content, tool_calls }`
  - `content`：模型的自然语言回复
  - `tool_calls`：工具调用请求列表，每项含 `id / name / arguments`

**关键设计点**：
- 不同厂商的工具调用格式不同（字段名、参数编码方式），必须在**这一层完成归一化**，上层感知不到差异。
- 支持的实现：OpenAI、Claude、通义、Ollama 等，各自一个适配器。
- 未来要支持流式输出，接口预留流式方法。

**为什么单独成层**：模型迭代极快，绑定任何一家都是技术债。

---

### 2. 工具层（Tools + Registry）

**职责**：定义 Agent 能做的事，并提供注册与查找机制。

**工具的三要素**（缺一不可）：
1. **名字**：模型调用时的唯一标识
2. **描述**：给模型看的说明书，决定模型会不会用、用得对不对
3. **参数 Schema**：JSON Schema 格式，告诉模型参数长什么样

**注册中心（Registry）**：
- 所有工具登记在册
- 提供三个能力：按名查找、导出全部 Schema、列出所有名字
- 核心层通过它把工具清单交给模型

**工具设计原则**：
- **永远返回字符串**：成功返回结果，失败返回错误说明，绝不抛异常打断主循环
- **描述要具体**：写清「什么时候用、参数什么含义」，模型靠这个决策
- **粒度适中**：太细模型要调很多次，太粗模型不会用
- **幂等优先**：能重复执行不产生副作用最好

**内置基础工具建议**：
- 计算器（安全求值，不用 eval）
- 时间/日期查询
- 文件读写
- 网页搜索（可选，需 API）
- HTTP 请求（可选）

---

### 3. 记忆层（Memory）

**职责**：管理对话历史，控制上下文长度。

**消息角色分类**：
- `system`：系统提示，定义行为约束
- `user`：用户输入
- `assistant`：模型回复（可能带工具调用）
- `tool`：工具执行结果，必须带 `tool_call_id` 关联到具体调用

**核心机制**：
- **追加**：新消息进来就加
- **裁剪**：超过阈值时，保留 system + 最近 N 轮，丢弃中间部分
- **清空**：保留 system，清掉其余

**进阶方向**（先留接口，不实现）：
- 摘要压缩：把旧对话用模型总结成一段话
- 向量长期记忆：历史存向量库，按需检索
- 分层记忆：短期（当前会话）+ 长期（跨会话）

**关键点**：`assistant` 带工具调用的消息和对应的 `tool` 结果消息，**必须成对保留或成对删除**，否则格式非法。

---

### 4. 核心层（Agent Core）

**职责**：整个项目的心脏，负责编排。

**主循环逻辑**（ReAct）：
1. 把「记忆 + 工具清单」交给模型
2. 判断模型输出：
   - **有工具调用** → 逐个执行 → 结果回填记忆 → 回到步骤 1
   - **无工具调用** → 得到最终答案 → 返回用户，结束
3. 每轮循环计数，达到**最大步数**强制退出

**关键设计点**：
- **最大步数兜底**：防止模型反复调同一工具或工具持续报错导致死循环
- **工具异常处理**：单个工具失败不影响整体，把错误信息作为结果回填
- **可观测性**：每步打印或记录日志（调用哪个工具、参数、结果），便于调试
- **verbose 开关**：控制是否输出中间过程

**为什么工具结果要回填成独立角色**：模型需要区分「自己说的」和「工具返回的」，格式正确模型才能正确推理。

---

### 5. 入口层（main）

**职责**：交互循环，最薄的一层。

**逻辑**：
- 初始化 LLM 和 Agent
- 循环读取用户输入
- 调用 Agent，打印结果
- 支持 `exit` / `quit` 退出，处理中断信号

**关键点**：入口层不写任何业务逻辑，只做交互。

---

## 四、数据流

```
用户输入
   ↓
写入记忆 (user)
   ↓
┌──────────────────────────────┐
│  核心循环（最多 N 步）          │
│                              │
│  记忆 + 工具Schema → 模型      │
│         ↓                    │
│  有工具调用？                  │
│   ├─ 是 → 执行工具             │
│   │      → 结果写入记忆 (tool) │
│   │      → 回到循环顶部        │
│   └─ 否 → 最终回答             │
│          → 写入记忆 (assistant)│
│          → 退出循环            │
└──────────────────────────────┘
   ↓
返回用户
```

---

## 五、目录结构

```
mini-agent/
├── agent/
│   ├── core.py          # Agent 主循环
│   ├── llm.py           # 模型抽象层 + 各厂商适配器
│   ├── memory.py        # 记忆管理
│   ├── prompt.py        # 提示词模板集中管理
│   └── tools/
│       ├── base.py      # 工具基类 + 注册中心
│       ├── calculator.py
│       ├── search.py
│       └── file_io.py
├── config.py            # 配置（模型、API Key、最大步数等）
├── main.py              # 入口
├── requirements.txt
└── README.md
```

**组织原则**：
- 每个文件单一职责
- 提示词集中在 `prompt.py`，不散落在代码里
- 工具一个文件一个，方便增删

---

## 六、配置设计

**配置项**：
- 模型名、API Key、Base URL（支持自定义端点）
- 最大步数（默认 8）
- 记忆最大轮数（默认 20）
- verbose 开关
- 超时设置

**管理方式**：
- 优先级：环境变量 > 配置文件 > 代码默认值
- API Key 绝不出现在代码里，只从环境变量读

---

## 七、提示词设计

**系统提示要解决的问题**：
1. **约束行为**：优先用工具，不要凭空猜测
2. **允许多步**：可以多次调用工具直到能回答
3. **明确终止**：得到答案就直接回复，不要多此一举再调工具
4. **风格统一**：语言、简洁度、准确性要求

**为什么提示词重要**：模型有偷懒倾向（能算却硬编），系统提示是行为约束，不是装饰。

---

## 八、关键设计决策与理由

| 决策 | 理由 |
|------|------|
| 四层分层 | 换模型、加工具、改记忆策略都只动一层 |
| 工具返回字符串 | 异常会打断循环，字符串能安全回填 |
| 最大步数兜底 | 防止模型死循环，必须有 |
| 工具描述独立 | 模型靠描述决策，描述质量=工具可用性 |
| 消息角色严格区分 | 格式错了模型理解就错 |
| 提示词集中管理 | 便于迭代调优，不散落 |
| 入口层最薄 | 交互和逻辑分离，便于换 UI（CLI/Web/API） |

---

## 九、扩展路线图（按优先级）

**第一阶段：跑通地基**
- 主循环稳定运行
- 2-3 个基础工具
- 基础记忆裁剪

**第二阶段：体验优化**
- 流式输出（最提升体感）
- 工具描述打磨
- 日志与可观测性

**第三阶段：能力增强**
- 规划层（任务先拆解再执行）
- 长期记忆 / RAG（向量库）
- 更多工具（HTTP、数据库、代码执行）

**第四阶段：架构升级**
- 多 Agent 协作（Agent 即 Tool）
- 并行工具调用
- Web UI / API 服务化

**优先级建议**：很多人一上来做 RAG，结果主循环不稳、工具描述粗糙，反而绕远路。**先把循环跑通、把工具描述写好，收益最大。**

---

## 十、常见坑与规避

| 坑 | 规避 |
|----|------|
| 工具抛异常打断循环 | 工具层 try/except，统一返回字符串 |
| 上下文无限增长 | 记忆层设阈值 + 裁剪策略 |
| 模型反复调同一工具 | 最大步数 + 提示词约束 |
| 工具描述太模糊模型不用 | 描述写清使用场景和参数含义 |
| assistant + tool 消息不成对 | 裁剪时按「调用组」处理 |
| API Key 硬编码 | 只从环境变量读 |
| 绑定单一模型 | 模型层抽象，适配器模式 |
| 提示词散落各处 | 集中到 prompt.py |

---

## 十一、验收标准

一个合格的基础 Agent 项目应满足：

1. 能完成「需要多步工具调用」的任务（如：先算 A，再用 A 查资料，最后总结）
2. 换一个 LLM 后端只改一处
3. 新增一个工具只写一个文件 + 注册
4. 上下文超长时不会崩溃
5. 工具报错时整体不崩，能继续或优雅结束
6. 整个过程有日志可追溯

---

## 十二、总结

这个方案的核心思想是**解耦 + 兜底 + 可观测**：

- **解耦**：四层各司其职，换任何一块不影响其他
- **兜底**：最大步数、工具异常、记忆裁剪，保证不崩
- **可观测**：每步可追踪，便于调试和优化

在此地基上，流式、规划、RAG、多 Agent 都是**增量叠加**，不需要推倒重来。这就是一个基础 Agent 项目该有的样子。

---
